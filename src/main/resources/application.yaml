server:
        port: 8080
        servlet:
                context-path: /momo

spring:
        datasource:
                url: "jdbc:mysql://localhost:3306/momoDb"
                username: root
                password: root
                driver-class-name: com.mysql.cj.jdbc.Driver
        jpa:
                hibernate:
                        ddl-auto: update
                show-sql: true
