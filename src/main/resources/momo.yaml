momo:
        api:
                url: ${MOMO_API_URL}
        secret:
                key: ${MOMO_SECRET_KEY}
        access:
                key: ${MOMO_ACCESS_KEY}
        return:
                url: ${MOMO_RETURN_URL}
        notify:
                url: ${MOMO_NOTIFY_URL}
        partner:
                code: ${MOMO_PARTNER_CODE}